spring:
  data:
    mongodb:
      host: localhost
      port: 0 # Use random port for embedded mongo in tests
      database: test_saude_mental

logging:
  level:
    com.saudemental.api: DEBUG
    org.springframework.data.mongodb: DEBUG
